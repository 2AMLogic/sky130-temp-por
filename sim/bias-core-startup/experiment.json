{
  "slug": "bias-core-startup",
  "title": "bias_core cold-start branch selection: transient supply ramp from 0 V across the full PVT matrix",
  "claim": "Issue #19 -- NOT a spec claim (spec/target-spec.md does not exist yet, spec/porting-plan.md Sec4 item 4). Substantiates that bias_core reaches its intended operating branch (VREF ~1.25 V, ~1 uA quiescent draw, BIAS_OK asserted) from a physical 0 V cold start at every point of the full 45-point PVT matrix -- i.e. that the on-die startup kick reliably breaks the degenerate zero-current state without any solver assistance. This is the branch-selection counterpart to sim/bias-core-smoke/, whose cold `.op` cannot make that claim: a `.op` has no initial state, so which branch it lands on is chosen by ngspice's gmin-stepping continuation rather than by the circuit. See sim/bias-core-op-branch/ for the measured evidence that the two spurious `.op` points in sim/bias-core-smoke/records/20260825-214036-a9cac4b.md are solver artifacts, not a second stable state.",
  "provenance": "schematic",
  "provenance_source": "sim/bias-core-startup/testbench/tb_bias_core_startup.sch",
  "schematic": "testbench/tb_bias_core_startup.sch",
  "statistical_convention": "N/A (corner-matrix check, not a distribution claim)",
  "corners": {
    "process": ["tt", "ss", "ff", "sf", "fs"],
    "temperature_c": [-40, 27, 125],
    "supply_v": [2.97, 3.3, 3.63]
  },
  "quick_subset": [
    ["tt", 27, 2.97],
    ["tt", 27, 3.63],
    ["ff", 125, 3.63]
  ],
  "deck": {
    "options": ["wnflag=1", "reltol=1e-4"],
    "params": {
      "tramp": "100u",
      "tstop": "10m"
    },
    "analyses": [
      "tran 10u 10m uic",
      "let nlast = length(v(vref))-1"
    ]
  },
  "measurements": [
    {
      "name": "vref",
      "expr": "v(vref)[nlast]",
      "unit": "V",
      "min": 1.0,
      "max": 1.4,
      "note": "Settled reference output at t=tstop, open circuit. Same harness-liveness window as sim/bias-core-smoke/'s own vref bound (deliberately a few hundred mV around design/bias_core.md's 1.2477-1.2574 V spot check), NOT a spec claim. The point of this experiment is WHICH branch is reached, not the exact value."
    },
    {
      "name": "vref_settled",
      "expr": "abs(v(vref)[nlast] - v(vref)[nlast-100])",
      "unit": "V",
      "min": 0.0,
      "max": 0.001,
      "note": "Settling guard: VREF must move less than 1 mV over the last 100 output steps (1 ms of the 10 ms window). Without this, a run that was still ramping at t=tstop could report an in-band VREF it had not actually settled to. nlast-100 is well past the 100 us supply ramp at every corner."
    },
    {
      "name": "ibias",
      "expr": "v(ibias)[nlast]",
      "unit": "V",
      "min": 2.0,
      "max": 3.7,
      "note": "IBIAS is open-circuit (no load in this testbench), so per design/bias_core.md's own compliance note it rails toward VDD. Window covers the full 2.97-3.63 V supply range with margin, matching sim/bias-core-smoke/'s bound."
    },
    {
      "name": "bias_ok",
      "expr": "v(bias_ok)[nlast]",
      "unit": "V",
      "min": 2.0,
      "max": 3.7,
      "note": "BIAS_OK active-high settle flag; must read rail-high once the core is up. In THIS experiment that is a real startup assertion, not just a liveness bound: BIAS_OK low at the end of a 0 V-cold-start ramp would mean the on-die kick failed to clear the degenerate zero-current state at that corner."
    },
    {
      "name": "isup",
      "expr": "-i(v1)[nlast]",
      "unit": "A",
      "min": 1e-8,
      "max": 1e-5,
      "note": "Total quiescent supply current at t=tstop. Same generous harness bound as sim/bias-core-smoke/. The lower bound also rejects the degenerate zero-current state, and the sign rejects the non-physical `.op` artifact this experiment exists to displace (that artifact reports a NEGATIVE supply current, i.e. the circuit sourcing milliamps into its own only energy source)."
    }
  ],
  "spread_checks": [
    {
      "measurement": "vref",
      "min_spread": 0.005,
      "note": "Same corner-blindness guard as sim/bias-core-smoke/: if every corner returns the same settled vref, the .lib/.temp injection is not taking effect."
    }
  ],
  "notes": [
    "Analyses list carries one non-analysis control line (`let nlast = length(v(vref))-1`). corner-run.py splices `deck.analyses` verbatim into the .control block ahead of the `let meas_* = <expr>` lines, so this is the supported way to define a helper vector the measurement expressions index against; nutmeg has no `last()` accessor.",
    "Ramp rate is fixed at 'vsup'/'tramp' = ~30 kV/s (100 us 0->3.3 V) for every corner. That is one point on the supply-ramp-rate axis, not coverage of it -- root CLAUDE.md's ramp-rate matrix requirement belongs to the POR block's own testbenches (spec/porting-plan.md Sec3), not to this bias-core branch-selection check. What this experiment claims is therefore branch selection at a representative ramp, not dv/dt immunity: a dedicated ramp-rate sweep on this cell (and on temp_por_top) is separate, later scope and is NOT claimed here.",
    "The PWL ramp is a fixed *duration* ('tramp' = 100 us) rather than a fixed dVDD/dt, so the ramp rate varies ~20% across the supply axis (29.7 kV/s at 2.97 V vs 36.3 kV/s at 3.63 V). That is the supply-vs-ramp-rate confound spec/porting-plan.md Sec3.3 warns about (carried from gf180's DR-021), and it is acceptable HERE only because this experiment's claim is binary branch selection at a representative ramp, not a rate-sensitive threshold. The POR block's own ramp-rate testbenches must derive the ramp from a constant dVDD/dt instead -- do not copy this schematic's supply source into them.",
    "`.option reltol=1e-4` (ngspice's default is 1e-3) is load-bearing, and was added only after being measured to be: the FIRST run of this experiment, record 20260825-235846-f4f73a5 -- committed, superseded, not deleted -- used the default and came back 43/45, with sf_125c_3.63v ending at VREF=0.171 V / BIAS_OK=0 V and fs_-40c_3.30v ending at VREF=3.298 V, BOTH reporting a NEGATIVE settled supply current (-5.57 mA and -3.17 mA). A negative supply current is the same impossible-state signature sim/bias-core-op-branch/ diagnoses in the cold `.op`: the transient's own per-timestep Newton solve had false-converged, silently, with no warning in the log. Re-running exactly those two decks with nothing changed but reltol=1e-4 puts both on the intended branch (VREF 1.2555 V / 1.2497 V, supply +1.18 uA / +0.77 uA). The tolerance is therefore part of the claim, not incidental: a `bias_core`-class deck with a ~1 uA operating current and 1e8-magnitude excursions available to its DeltaVBE nodes is not safely converged at reltol=1e-3.",
    "`uic` is what makes this a cold start: it skips the initial operating-point solve entirely and integrates forward from all-zero initial conditions. Without `uic`, ngspice would compute a `.op` at t=0 first, reintroducing exactly the solver-chosen initial guess this experiment exists to avoid."
  ]
}

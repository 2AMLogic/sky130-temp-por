{
  "family": "sky130",
  "variant": "sky130A",
  "open_pdks_commit": "c6d73a35f524070e85faff4a6a9eef49553ebc2b",
  "install_command": "volare enable --pdk sky130 c6d73a35f524070e85faff4a6a9eef49553ebc2b",
  "default_pdk_root": "~/.volare",
  "ngspice_lib": "libs.tech/combined/sky130.lib.spice",
  "xschem_pdk_rc": "libs.tech/xschem/xschemrc",
  "process_corners": ["tt", "ss", "ff", "sf", "fs", "ll", "hh"],
  "notes": [
    "Ported verbatim from sky130-bandgap/sim/pdk.json (issue #17) -- this",
    "repo's harness reuses that repo's already-verified PDK reproducibility",
    "pin rather than re-deriving it. Re-confirmed against THIS repo's own",
    "installed PDK at port time (2026-08-25): $PDK_ROOT/sky130A resolves",
    "(via its volare symlink) to open_pdks commit",
    "c6d73a35f524070e85faff4a6a9eef49553ebc2b -- an exact match, not assumed",
    "from sky130-bandgap's own pin.",
    "This file is the reproducibility pin for every record under sim/.",
    "open_pdks_commit is the volare version hash for the sky130 PDK family;",
    "the corner runner reads $PDK_ROOT/$PDK back through its symlink and",
    "refuses to run against a different version unless --allow-pdk-mismatch",
    "is passed (in which case the record carries the mismatch).",
    "process_corners are the basic N/P device sections that actually exist in",
    "the PDK ngspice library (verified against the .lib sections of",
    "libs.tech/combined/sky130.lib.spice). tt/ss/ff/sf/fs are the standard",
    "MOS/BJT (nonfet.spice-driven) corners -- they also gate PNP Is/Bf/Nf via",
    "the same corners/<name>/nonfet.spice includes. ll/hh (added for #4's",
    "resistor-flavor characterization) are the resistor/capacitor-only skew",
    "corners: they ride on the nominal tt MOS/BJT corner (all ll/hh/hl/lh",
    "'.lib' sections '.include corners/tt.spice') and instead vary",
    "parameters_res_low.spice / parameters_res_high.spice, which is where",
    "sky130_fd_pr__res_high_po / __res_xhigh_po sheet resistance actually",
    "changes -- confirmed empirically: the standard tt/ss/ff/sf/fs corners",
    "give byte-identical resistor values (all five include",
    "parameters_res_nom.spice), so resistor-value process sensitivity only",
    "shows up on the ll/hh axis. hl/lh (res value tied to the *other*",
    "axis's cap corner) and the *_mm / mc mismatch sections also exist and",
    "remain available to experiment manifests that need them but are not",
    "added to this pinned list until a manifest actually uses them.",
    "The *_mm sections are the local-mismatch switch: they differ from the",
    "plain corners only in setting MC_MM_SWITCH=1, which enables the",
    "per-instance AGAUSS() terms inside the device subcircuits (mc sets",
    "MC_PR_SWITCH=1 instead, for global process Monte Carlo). tt_mm is in",
    "use by sim/pnp-mismatch/, which drives ngspice itself rather than",
    "going through an experiment.json -- Monte Carlo sampling is not a",
    "corner axis -- so it is deliberately still absent from the list above:",
    "corner-run.py validates manifest corners against that list, and a",
    "manifest asking for a *_mm section would get one unrepeated random",
    "draw per point rather than a distribution."
  ]
}
